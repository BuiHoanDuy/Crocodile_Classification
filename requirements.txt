fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0
python-multipart>=0.0.6

# Note: Với Python 3.13, nếu gặp lỗi khi cài đặt, hãy cài từ wheel:
# python -m pip install --only-binary :all: -r requirements.txt
# 
# Hoặc cài từng package:
# python -m pip install --only-binary :all: scikit-learn pydantic-core
# python -m pip install fastapi uvicorn[standard] pydantic pandas numpy joblib python-multipart

